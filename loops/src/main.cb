namespace App {
  using Console;

  export unsafe fn main(): int {
    iterator -> iterate [increment(3i)] (index) -> "Performing a loop ";

    message -> reduce[iterator, ""] (item, current) -> current.concat(item);

    return message.Log();
  }
}